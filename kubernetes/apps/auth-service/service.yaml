apiVersion: v1
kind: Service
metadata:
  name: auth-service
  labels:
    app: auth-service
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: auth-service
  type: NodePort
