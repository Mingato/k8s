apiVersion: v1
kind: Service
metadata:
  name: socket-service
  labels:
    app: socket-service
spec:
  type: NodePort
  selector:
    app: socket-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080