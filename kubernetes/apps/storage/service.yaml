apiVersion: v1
kind: Service
metadata:
  name: storage-service
  labels:
    app: storage-service
spec:
  type: NodePort
  selector:
    app: storage-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080