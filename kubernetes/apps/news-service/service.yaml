apiVersion: v1
kind: Service
metadata:
  name: news-service
  labels:
    app: news-service
spec:
  type: NodePort
  selector:
    app: news-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080